meta {
  name: 01 List Questionnaires
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/questionnaires
  body: none
  auth: basic
}

auth:basic {
  username: {{username}}
  password: {{password}}
}

tests {
  test("returns an array", function () {
    expect(res.status).to.equal(200);
    expect(res.body).to.be.an("array");
  });
}
